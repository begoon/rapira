(* Множества: автоматическое объединение совпадающих элементов и операции. *)

<* 1, 1, 2, 3, 3 *> -> А;
<* 3, 4, 5 *>        -> Б;
? А;
? А + Б;       (* объединение *)
? А * Б;       (* пересечение *)
? А - Б;       (* разность   *)
? 3 ИЗ А;      (* принадлежность *)
? 9 ИЗ А;
